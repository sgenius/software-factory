{
  "name": "software-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Generic multi-agent pipeline (planner/coder/tester/reviewer) for producing and reviewing code.",
  "type": "module",
  "scripts": {
    "build": "tsc -p orchestrator/tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
